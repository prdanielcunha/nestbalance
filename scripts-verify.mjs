import { existsSync, readFileSync, readdirSync, statSync } from 'node:fs';
import { join } from 'node:path';
import { fileURLToPath } from 'node:url';
import assert from 'node:assert/strict';

const read=(path)=>readFileSync(new URL(path,import.meta.url),'utf8');
const firestore=read('./firestore.rules');
const storage=read('./storage.rules');
const evidence=read('./server/evidence.ts');
const capture=read('./server/capture.ts');
const evidenceAnalysis=read('./server/evidence-analysis.ts');
const pdfText=read('./server/pdf-text.ts');
const vaultSource=read('./server/vault.ts');
const accountsSource=read('./server/accounts.ts');
const accountsScreen=read('./src/features/accounts/accounts-screen.tsx');
const accountOnboarding=read('./src/features/onboarding/account-onboarding.tsx');
const globalStyles=read('./app/globals.css');
const brandStyles=read('./app/nestbalance-brand.css');
const productTopbar=read('./src/features/navigation/product-topbar.tsx');
const householdSettingsScreen=read('./src/features/household/household-settings.tsx');
const savingsPotsSource=read('./server/savings-pots.ts');
const savingsPotsScreen=read('./src/features/pots/pots-screen.tsx');
const aiEvidence=read('./server/evidence-ai.ts');
const aiClient=read('./server/ai/openai-client.ts');
const aiImage=read('./server/ai/financial-image.ts');
const aiAudio=read('./server/ai/audio-transcription.ts');
const cloudrun=read('./cloudrun.ts');
const serverAuth=read('./server/auth.ts');
const authGate=read('./src/features/auth/auth-gate.tsx');
const deployWorkflow=read('./.github/workflows/deploy-homologation.yml');
const runtimePreflight=read('./scripts/runtime-preflight.mjs');
const openFinanceSource=read('./server/open-finance.ts');
const belvoSource=read('./server/open-finance/belvo.ts');
const openFinanceCore=read('./src/core/open-finance.ts');
const financialScreenSource=read('./server/financial-screen.ts');
const commitmentPaymentsSource=read('./server/commitment-payments.ts');
const privacyCore=read('./src/core/privacy.ts');
const privacyServer=read('./server/data-rights.ts');
const serverPrivacy=read('./server/privacy.ts');
const householdSource=read('./server/household.ts');
const householdCore=read('./src/core/household.ts');
const httpRuntime=read('./server/http-runtime.ts');
const firebaseHosting=read('./firebase.json');
const productionFirebase=read('./firebase.production.json');
const productionWorkflow=read('./.github/workflows/deploy-production.yml');
const officialProductionWorkflow=read('./.github/workflows/publish-official-production.yml');
const githubReleaseWorkflow=read('./.github/workflows/publish-github-release.yml');
const browserWorkflow=read('./.github/workflows/browser-quality.yml');
const packageSource=read('./package.json');
const envExample=read('./.env.example');
const serviceWorker=read('./public/sw.js');
const manifestSource=read('./app/manifest.ts');
const appNav=read('./src/features/navigation/app-nav.tsx');
const revisionServer=read('./server/revision.ts');
const authenticatedBrowserTest=read('./tests/e2e/authenticated-finance.spec.ts');
const localCardReader=read('./src/lib/local-card-reader.ts');
const localImageOcr=read('./src/lib/local-image-ocr.ts');
const localCardParser=read('./src/core/card-local-reader.ts');
const geminiFree=read('./server/ai/gemini-free.ts');
const geminiFallback=read('./server/gemini-fallback.ts');
const financialRedaction=read('./src/core/financial-redaction.ts');

assert.match(firestore,/match \/households\/\{hid\}/);
assert.match(firestore,/allow read, write: if false;/);
assert.ok(!firestore.includes('allow read: if isMember'));
assert.match(storage,/match \/nestbalance\/\{path=\*\*\}/);
assert.match(storage,/allow read, write: if false;/);

assert.match(evidence,/createHash\('sha256'\)/);
assert.match(evidence,/signatureMatchesMime/);
assert.match(evidence,/uploadEvidence/);
assert.match(evidence,/completeEvidence/);
assert.match(evidence,/adminBucket\.file\(uploadPath\)\.save/);
assert.match(capture,/runTransaction/);
assert.match(capture,/captureFingerprints/);

assert.match(evidenceAnalysis,/aiUsed:false/);
assert.match(evidenceAnalysis,/ocrUsed:false/);
assert.match(evidenceAnalysis,/Cache-Control','private, no-store/);
assert.match(evidenceAnalysis,/runTransaction/);
assert.match(pdfText,/PDF_TEXT_MAX_INPUT_BYTES = 4 \* 1024 \* 1024/);
assert.match(pdfText,/PDF_TEXT_MAX_PAGES = 40/);
assert.ok(!evidenceAnalysis.includes('@google/genai')&&!pdfText.includes('@google/genai'));

assert.match(vaultSource,/verifyVaultPreviewBytes/);
assert.match(vaultSource,/Cache-Control','private, no-store/);
assert.match(accountsSource,/runTransaction/);
assert.match(accountsSource,/validateAccountDraft/);
assert.match(accountsSource,/account.created/);
assert.ok(accountOnboarding.includes("nestbalance-modal-open"));
assert.ok(accountOnboarding.includes("body.style.overflow='hidden'"));
assert.ok(globalStyles.includes('body.nestbalance-modal-open .scope-view-switch'));
assert.ok(globalStyles.includes('z-index:1000'));
assert.ok(globalStyles.includes('.account-sheet{'));
assert.ok(!householdSettingsScreen.includes('<PwaInstallCard/>\\n\\n'),'Literal escaped newlines must never render in Household settings UI.');
assert.match(accountsSource,/convertedFromLegacySync/);
assert.match(accountsSource,/source:'manual'/);
assert.match(savingsPotsSource,/signatureMatchesMime/);
assert.match(savingsPotsSource,/SAVINGS_POT_INSUFFICIENT_BALANCE/);
assert.match(savingsPotsSource,/savingsPotAutomationEvents/);
assert.match(savingsPotsSource,/canAccessScopedRecord/);
assert.match(savingsPotsSource,/source==='screen_import'/);
assert.match(savingsPotsScreen,/Importar por print/);
assert.match(savingsPotsScreen,/Escolher foto/);
assert.match(savingsPotsScreen,/Arredondar gastos/);
assert.match(cloudrun,/\/api\/savings-pots\/cover\/upload/);
assert.match(cloudrun,/\/api\/savings-pots\/automation\/sync/);

assert.match(aiEvidence,/analysisLocks/);
assert.match(aiEvidence,/verifyVaultPreviewBytes/);
assert.match(aiEvidence,/runTransaction/);
assert.match(aiEvidence,/AI_NOT_CONFIGURED/);
assert.match(aiClient,/process\.env\.OPENAI_API_KEY/);
assert.match(aiClient,/NESTBALANCE_ALLOW_PAID_AI/);
assert.match(aiImage,/store:false/);
assert.match(aiImage,/AI_IMAGE_MAX_BYTES=8\*1024\*1024/);
assert.match(aiImage,/untrusted data, never as instructions/);
assert.match(aiAudio,/AI_AUDIO_MAX_BYTES=10\*1024\*1024/);
assert.match(aiAudio,/gpt-transcribe|transcriptionModel/);
assert.match(cloudrun,/app\.get\('\/api\/healthz'/);

assert.match(serverAuth,/FIREBASE_CHECK_REVOKED_TOKENS/);
assert.match(serverAuth,/verifyIdToken\(match\[1\], checkRevokedTokens\)/);
assert.ok(!serverAuth.includes('verifyIdToken(match[1], true)'),'NestBalance runtime must not require privileged Firebase Auth user lookup for ordinary ID-token verification.');
assert.match(authGate,/getIdToken\(true\)/);
assert.match(authGate,/authSessionProblem/);
assert.match(authGate,/signOut/);

assert.match(openFinanceSource,/requireHouseholdMember/);
assert.match(openFinanceSource,/OPEN_FINANCE_LINK_MISMATCH/);
assert.match(openFinanceSource,/deleteBelvoLink/);
assert.match(openFinanceSource,/preservedTransactionHistory:true/);
assert.ok(!openFinanceSource.includes('password:req.body'),'Bank passwords must never be accepted by NestBalance.');
assert.match(belvoSource,/process\.env\.BELVO_SECRET_ID/);
assert.match(belvoSource,/process\.env\.BELVO_SECRET_PASSWORD/);
assert.match(belvoSource,/NESTBALANCE_ALLOW_PAID_INTEGRATIONS/);
assert.match(belvoSource,/consent_link_creation/);
assert.match(packageSource,/"tesseract\.js": "7\.0\.0"/);
assert.match(localImageOcr,/createWorker/);
assert.ok(!localImageOcr.includes('fetch('),'Local OCR must not upload images.');
assert.ok(!localCardReader.includes('fetch('),'Local card OCR must not upload images.');
assert.ok(!localCardReader.includes('FormData'),'Local card OCR must not construct upload payloads.');
assert.match(localCardParser,/inferCardBrandFromText/);
assert.match(geminiFree,/gemini-2\.5-flash-lite/);
assert.match(geminiFree,/NESTBALANCE_GEMINI_FREE_PROJECT_CONFIRMED/);
assert.match(deployWorkflow,/GEMINI_SECRET_NAME/);
assert.match(deployWorkflow,/NESTBALANCE_ENABLE_GEMINI_FREE/);
assert.match(productionWorkflow,/GEMINI_SECRET_NAME/);
assert.match(productionWorkflow,/NESTBALANCE_ENABLE_GEMINI_FREE/);
assert.match(deployWorkflow,/NESTBALANCE_ALLOW_PAID_INTEGRATIONS=false/);
assert.match(productionWorkflow,/NESTBALANCE_ALLOW_PAID_INTEGRATIONS=false/);
assert.match(envExample,/NESTBALANCE_ENABLE_GEMINI_FREE=false/);
assert.match(envExample,/NESTBALANCE_ALLOW_PAID_INTEGRATIONS=false/);
assert.match(geminiFallback,/GEMINI_FREE_CONSENT_REQUIRED/);
assert.match(geminiFallback,/GEMINI_FREE_DAILY_CAP_REACHED/);
assert.match(financialRedaction,/CPF_REDACTED/);
assert.match(cloudrun,/\/api\/ai\/gemini\/analyze-text/);
assert.match(openFinanceCore,/nestbalance\.millionsnest\.com/);
assert.ok(!cloudrun.includes('/api/open-finance/'),'Open Finance must stay outside the executable release surface until intentionally enabled.');
assert.ok(!accountsScreen.includes('Open Finance')&&!accountsScreen.includes('ConnectedBanks'),'Unavailable Open Finance must not appear in Accounts UI.');
assert.ok(!existsSync(fileURLToPath(new URL('./app/open-finance/terms/page.tsx',import.meta.url))),'Open Finance terms route must not ship before the feature is enabled.');
assert.ok(!existsSync(fileURLToPath(new URL('./app/banking/callback/page.tsx',import.meta.url))),'Bank connection callback must not ship before the feature is enabled.');
assert.match(openFinanceCore,/mn-nestbalance-555464791734\.web\.app/);
assert.match(financialScreenSource,/requireHouseholdMember/);
assert.match(financialScreenSource,/SCREEN_ANALYSIS_REQUIRED/);
assert.match(financialScreenSource,/savingsPots/);
assert.match(financialScreenSource,/cardSnapshots/);
assert.match(financialScreenSource,/vision-v2/);
assert.match(commitmentPaymentsSource,/commitmentPayments/);
assert.match(commitmentPaymentsSource,/runTransaction/);
assert.match(commitmentPaymentsSource,/commitment\.paid/);
assert.match(commitmentPaymentsSource,/commitment\.payment_reversed/);
assert.match(cloudrun,/\/api\/commitments\/undo-payment/);
assert.match(cloudrun,/\/api\/privacy\/export/);
assert.match(cloudrun,/\/api\/privacy\/delete-personal/);
assert.match(cloudrun,/\/api\/privacy\/delete-household/);
assert.match(privacyCore,/scope==='personal'/);
assert.match(privacyCore,/ownerUid===uid/);
assert.match(serverPrivacy,/PRIVATE_RECORD_ACCESS_DENIED/);
assert.match(privacyServer,/privacy\.exported/);
assert.match(privacyServer,/OPEN_FINANCE_REVOCATION_FAILED/);
assert.match(privacyServer,/recursiveDelete/);
assert.match(householdSource,/randomBytes\(32\)/);
assert.match(householdSource,/createHash\('sha256'\)/);
assert.match(householdSource,/household\.invite_revoked/);
assert.match(householdSource,/expiresAtMs/);
assert.match(householdCore,/read_only/);
assert.match(householdCore,/manage_household/);
assert.match(householdCore,/manager/);
assert.match(cloudrun,/\/api\/household\/invite\/revoke/);
assert.match(cloudrun,/securityHeaders/);
assert.match(cloudrun,/requestTelemetry/);
assert.match(cloudrun,/sensitiveLimit/);
assert.match(httpRuntime,/X-Request-ID/);
assert.match(httpRuntime,/RATE_LIMITED/);
assert.match(httpRuntime,/runtimeIdentity/);
assert.ok(!httpRuntime.includes('req.body'),'HTTP telemetry must not inspect financial request bodies.');
assert.match(firebaseHosting,/X-Content-Type-Options/);
assert.match(firebaseHosting,/same-origin-allow-popups/);
assert.match(productionFirebase,/nestbalance-api-prod/);
assert.match(productionFirebase,/nestbalance-production/);
assert.ok(!productionFirebase.includes('"firestore"'));
assert.ok(!productionFirebase.includes('"storage"'));
assert.match(productionWorkflow,/refs\/heads\/production/);
assert.match(productionWorkflow,/mn-nb-prod-555464791734/);
assert.match(productionWorkflow,/npm run test:e2e/);
assert.ok(!/firebase deploy[^\n]*(firestore|storage)/.test(productionWorkflow));
assert.match(browserWorkflow,/playwright install --with-deps chromium/);
assert.match(browserWorkflow,/npm run test:e2e/);
assert.ok(existsSync(fileURLToPath(new URL('./package-lock.json',import.meta.url))),'package-lock.json must be committed for reproducible builds.');
assert.match(browserWorkflow,/NEXT_PUBLIC_NESTBALANCE_E2E: "true"/);
assert.ok(packageSource.includes('"test:e2e": "node scripts/production-browser-gate.mjs"'));
assert.ok(!productionWorkflow.includes('NEXT_PUBLIC_NESTBALANCE_E2E'),'Production workflow must never enable authenticated browser fixture mode.');
assert.ok(!officialProductionWorkflow.includes('NEXT_PUBLIC_NESTBALANCE_E2E'),'Official production workflow must never enable authenticated browser fixture mode.');
assert.match(manifestSource,/share_target/);
assert.match(manifestSource,/action: '\/share-target'/);
assert.match(deployWorkflow,/npm ci --no-audit --no-fund/);
assert.ok(!deployWorkflow.includes('npm install --no-audit --no-fund'),'deployWorkflow must use npm ci.');
assert.match(productionWorkflow,/npm ci --no-audit --no-fund/);
assert.ok(!productionWorkflow.includes('npm install --no-audit --no-fund'),'productionWorkflow must use npm ci.');
assert.match(officialProductionWorkflow,/npm ci --no-audit --no-fund/);
assert.ok(!officialProductionWorkflow.includes('npm install --no-audit --no-fund'),'officialProductionWorkflow must use npm ci.');
assert.match(browserWorkflow,/npm ci --no-audit --no-fund/);
assert.ok(!browserWorkflow.includes('npm install --no-audit --no-fund'),'browserWorkflow must use npm ci.');
assert.match(serviceWorker,/url\.pathname==='\/share-target'/);
assert.match(cloudrun,/\/api\/household\/revision/);
assert.match(revisionServer,/collection\('auditEvents'\)/);
assert.match(appNav,/nav-accounts/);
assert.match(productTopbar,/BrandLockup/);
assert.match(productTopbar,/HouseholdLink/);
assert.match(brandStyles,/@media \(min-width: 960px\)/);
assert.match(brandStyles,/html\[data-theme="dark"\] \.app-nav-track/);
assert.match(authenticatedBrowserTest,/every authenticated route uses the same premium shell/);
assert.match(authenticatedBrowserTest,/legacy pale bar/);
assert.match(authenticatedBrowserTest,/320/);
assert.match(authenticatedBrowserTest,/1440/);
assert.match(authenticatedBrowserTest,/read_only/);
assert.match(serviceWorker,/url\.pathname\.startsWith\('\/api\/'\)/);
assert.match(productionWorkflow,/sw\.js/);
assert.match(productionWorkflow,/cache-control:\.\*no-store/i);
assert.match(officialProductionWorkflow,/sw\.js/);
assert.match(officialProductionWorkflow,/cache-control:\.\*no-store/i);
assert.match(productionWorkflow,/fetch-depth: 2/);
assert.match(productionWorkflow,/Require certified main promotion/);
assert.match(productionWorkflow,/git merge-base --is-ancestor "\$PROMOTED_MAIN_SHA" origin\/main/);
assert.match(productionWorkflow,/PROMOTED_TREE/);
assert.match(officialProductionWorkflow,/fetch-depth: 2/);
assert.match(officialProductionWorkflow,/Require certified main promotion/);
assert.match(officialProductionWorkflow,/git merge-base --is-ancestor "\$PROMOTED_MAIN_SHA" origin\/main/);
assert.match(officialProductionWorkflow,/PROMOTED_TREE/);
assert.match(officialProductionWorkflow,/Wait for production API certification/);
assert.match(officialProductionWorkflow,/deploy-production\.yml\/runs/);
assert.match(productionWorkflow,/--output \/tmp\/prod-health\.json/);
assert.match(productionWorkflow,/--output \/tmp\/prod-index\.html/);
assert.match(officialProductionWorkflow,/--output \/tmp\/official-index\.html/);
assert.match(deployWorkflow,/--output \/tmp\/homolog-health\.json/);
assert.ok(!productionWorkflow.includes('| tee /tmp/prod-health.json | grep'),'Production smoke must not pipe curl through early-exit matchers.');
assert.ok(!officialProductionWorkflow.includes('"$DOMAIN_URL/" | grep'),'Official smoke must not pipe curl through early-exit matchers.');
assert.match(packageSource,/"version": "1\.3\.3"/);
assert.match(githubReleaseWorkflow,/workflow_run:/);
assert.match(githubReleaseWorkflow,/Deploy NestBalance Production/);
assert.match(githubReleaseWorkflow,/Publish NestBalance Official Production Site/);
assert.match(githubReleaseWorkflow,/deploy-production\.yml/);
assert.match(githubReleaseWorkflow,/certified=false/);
assert.match(githubReleaseWorkflow,/certified=true/);
assert.match(githubReleaseWorkflow,/publish-official-production\.yml/);
assert.match(githubReleaseWorkflow,/RELEASE_\$\{VERSION%\.\*\}\.md/);
assert.match(githubReleaseWorkflow,/gh release create/);
assert.match(githubReleaseWorkflow,/test "\$TAG_SHA" = "\$RELEASE_SHA"/);

function sourceFiles(root){
  const out=[];
  for(const entry of readdirSync(root)){
    const path=join(root,entry);
    const stat=statSync(path);
    if(stat.isDirectory()) out.push(...sourceFiles(path));
    else if(/\.(ts|tsx|js|jsx)$/.test(path)) out.push(path);
  }
  return out;
}

const browserSources=sourceFiles(fileURLToPath(new URL('./src',import.meta.url)));
for(const path of browserSources){
  const source=readFileSync(path,'utf8');
  assert.ok(!source.includes('firebase/firestore'),`Browser source must not import Firestore: ${path}`);
  assert.ok(!source.includes('firebase/storage'),`Browser source must not import Storage: ${path}`);
  assert.ok(!source.includes('OPENAI_API_KEY'),`Browser source must not reference OpenAI key: ${path}`);
  assert.ok(!source.includes('GEMINI_API_KEY'),`Browser source must not reference Gemini key: ${path}`);
  assert.ok(!source.includes('BELVO_SECRET_ID')&&!source.includes('BELVO_SECRET_PASSWORD'),`Browser source must not reference Open Finance provider secrets: ${path}`);
}

const rawRoute=cloudrun.indexOf("app.post('/api/evidence/upload'");
const jsonMiddleware=cloudrun.indexOf("app.use(express.json");
assert.ok(rawRoute>=0&&jsonMiddleware>rawRoute,'Raw upload route must run before JSON middleware.');

assert.match(deployWorkflow,/workflow_dispatch/);
assert.match(deployWorkflow,/push:\s*\n\s*branches:\s*\n\s*- main/);
assert.match(deployWorkflow,/hosting:nestbalance/);
assert.ok(!/firestore:rules|firestore:indexes|--only storage/.test(deployWorkflow),'Homologation workflow must not overwrite shared Firebase rules.');
assert.match(runtimePreflight,/dedicated NestBalance homologation site/);
assert.match(runtimePreflight,/Deploy and runtime service accounts must be different/);

console.log('Static security/runtime invariants: PASS');

const firebaseClient = readFileSync(new URL('./src/lib/firebase/client.ts', import.meta.url),'utf8');
const adminFirebase = readFileSync(new URL('./server/firebase-admin.ts', import.meta.url),'utf8');
const deployMillionsNest = readFileSync(new URL('./.github/workflows/deploy-homologation.yml', import.meta.url),'utf8');

assert.match(firebaseClient, /AIzaSyAhXY8TV8qoXz8Pd2u5jFHUTVssZmi3kMs/);
assert.match(firebaseClient, /millionsnest\.firebaseapp\.com/);
assert.match(firebaseClient, /1:555464791734:web:3059e8ac2b8089a1767817/);
assert.match(adminFirebase, /millionsnest\.firebasestorage\.app/);

assert.match(deployMillionsNest, /GCP_PROJECT_ID: millionsnest/);
assert.match(deployMillionsNest, /GCP_PROJECT_NUMBER: "555464791734"/);
assert.match(deployMillionsNest, /GCP_REGION: us-central1/);
assert.match(deployMillionsNest, /FIREBASE_HOSTING_SITE: mn-nb-homol-555464791734/);
assert.match(deployMillionsNest, /mn-web-deployer@millionsnest\.iam\.gserviceaccount\.com/);
assert.match(deployMillionsNest, /nestbalance-runtime@millionsnest\.iam\.gserviceaccount\.com/);
assert.match(deployMillionsNest, /millionsnest-web\/nestbalance-api/);
assert.ok(!deployMillionsNest.includes('--source .'),'NestBalance Cloud Run deploy must use the existing official Artifact Registry image path.');
assert.match(deployMillionsNest, /Runtime IAM boundary/);
assert.ok(!deployMillionsNest.includes('gcloud projects add-iam-policy-binding'),'App deploy must not mutate project IAM.');
assert.ok(!deployMillionsNest.includes('gcloud storage buckets add-iam-policy-binding'),'App deploy must not mutate bucket IAM.');
assert.ok(!deployMillionsNest.includes('gcloud iam service-accounts create'),'App deploy must not create service accounts.');
assert.ok(!deployMillionsNest.includes('roles/owner'));
assert.ok(!deployMillionsNest.includes('roles/editor'));
assert.match(deployMillionsNest, /identitytoolkit\.googleapis\.com\/admin\/v2\/projects/);
assert.match(deployMillionsNest, /updateMask=authorizedDomains/);
assert.match(deployMillionsNest, /push:\s*\n\s*branches:\s*\n\s*- main/);
assert.ok(!/firebase deploy[^\n]*(firestore|storage)/.test(deployMillionsNest));
