rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /nestbalance/{path=**} {
      // Originals are uploaded/read only by nestbalance-api via Admin credentials.
      allow read, write: if false;
    }
  }
}
